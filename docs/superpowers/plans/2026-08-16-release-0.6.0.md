# Release 0.6.0 — the exhaustion loop

**Started 2026-08-16 01:35Z.** No fixed deadline: the operator set a *ladder*, not a clock. The loop
runs until the ladder is exhausted or the operator stops it. Run `date -u` to check the clock.
**Do not infer the current time from git history.**

## The operator's instruction, in their words

> go through any/all docs and build any features that are not built without exception. if you can't
> find anything, do some research on other civic platforms and do a feature parity matrix and
> document that so future agents can find work. if that is exhausted then review code for security
> and test coverage to ensure we have a secure and trustworthy public facing platform and product.
> if that is exhausted the do a design review and do mockups that would fix any UI issues or
> otherwise improve UI/UX.

Also: **token-efficient delegation to lesser models as I deem appropriate.**

## The ladder

Each rung is attempted only when the one above it is genuinely exhausted — and "exhausted" must be
*demonstrated in this file*, not asserted. A rung skipped for convenience is the same failure this
project exists to refuse about the public record.

1. **Build the documented-but-unbuilt.** Every doc, without exception.
2. **Feature parity matrix** against other civic-transparency platforms — research, then document,
   so a future agent finds work here instead of re-deriving it.
3. **Security and test-coverage review** of the public-facing surface.
4. **Design review + UI/UX mockups.**

## Standing rules

Carried from the 0.4.0 and 0.5.0 loops. These are what kept those loops honest.

- **One dev subagent at a time.** No nesting. The main loop plans, verifies, commits, and answers
  the subagent via SendMessage.
- **The subagent never runs a git write command.** No `add`, `commit`, `checkout`, `stash`, `reset`.
  Read-only git is fine. The main loop commits.
- **`git add -A` is banned while an agent is running** — it would sweep the agent's half-finished
  work into a commit that claims to be something else.
- **Verify with real command output.** Never an agent's claim that it passes. `blocked` is not
  `pass`.
- **Never run the backend suite from the main loop while an agent may hold the test DB.**
- **Mutation-verify every guard**, in both directions: it must be *seen* to fail when the thing it
  guards is broken, and seen to pass when it is not.
- **Commit messages go in a file and are passed with `-F`.** Backticks and double quotes inside
  `git commit -m` are shell substitution; two 0.5.0 messages were silently eaten and the commit
  still exited 0.
- **Push to `origin` (Gitea, drives CI) and `github` (mirror, does not).**
- **Anything that changes what is published about a named person, or withdraws live public content,
  stops and goes to the operator.** It gets a spec, not an implementation.

## Model delegation policy

The operator asked for token efficiency. The split:

| Work | Model | Why |
|---|---|---|
| Doc sweeps, inventories, grep-and-report | `sonnet` | Mechanical breadth; no judgement about publication |
| Mechanical implementation against a precise spec | `sonnet` | The spec carries the judgement |
| Verification runs (typecheck/test/build) | main loop, no agent | An agent reporting its own pass is the thing we don't accept |
| Anything touching publication, the review gate, or a person's name | main loop, `opus` | Non-delegable |
| Security review, threat reasoning | `opus` | Getting this wrong is silent |

**The rule under the table:** delegate *breadth*, never *judgement*. A cheaper model reading fifty
files and reporting what it found is a good trade. A cheaper model deciding whether something is
safe to publish is not a trade at all.

## What the survey found

A sonnet sweep of every doc, every plan checkbox, and a TODO/FIXME grep of both source trees. The
grep found **nothing** — no TODO, FIXME, XXX or "not implemented" anywhere in `backend/src` or
`frontend/src`. The only hits were the literal `CW-XXXXXXXX` in a reference-ID docstring and the word
"stub" describing an intentional 8-byte empty-VTT sentinel. **This codebase does not carry its
backlog in comments; it carries it in docs.** Which is why the ladder starts where it does.

Three buckets:

- **A — buildable now, no publication impact.** 10 real items.
- **B — requires an operator decision.** 16 items, of which 12 touch either what is published about a
  named official or a live public surface.
- **C — already built despite a doc saying otherwise.** 8 items, the important discovery below.

### The stale-checkbox hazard (C)

**Six** plan files carry unchecked `[ ]` boxes describing work that **shipped weeks ago** — the
survey said eight, having folded two roadmap-spec items into the count; the corrected figure is six
files and **144** boxes: operator
auth (~29 boxes, closed 2026-08-09), full-text search (7 boxes, landed via migration 035),
subscriptions/delivery (27 boxes, all built and shipped dark), tier-A foundations (22 boxes), W0 and
W4 checklists, and three "Tier 6 — reach" items in the 2026-08-14 roadmap (`export/ocd.ts`,
`BotPage.tsx`, `export/receipt.ts`) that all exist in code right now.

This is a live trap, not untidiness. **An unchecked box is an instruction to a future agent**, and the
next loop that reads one of these will rebuild something that already works — against a codebase
where a second implementation of an existing service is precisely how the deleted `agents/` directory
became a second, worse analysis engine writing to the same table. The boxes were never ticked because
nobody goes back; the fix is therefore not to tick them but to mark the file superseded, which is what
the 0.5.0 plan header did when its own premise expired.

## Tasks

### Rung 1 — build the documented-but-unbuilt

- [x] **H1 — test-suite event-log leaks and the duplicated cursor helper.** **Already done**, by
      `2c7fdda` in the previous loop. Leaks re-measured at **zero**, not 155; one helper definition,
      not two. Re-mutation-verified anyway: removing `clearSuiteDisputes()` makes the check exit 1
      and name the leak. The 0.5.0 plan's G8a/G8b boxes were stale and are now ticked with their
      evidence.

      **This is H2's hazard catching the loop that fixed H2.** I read an unchecked box, believed it,
      and spent an agent and nine minutes rediscovering that the work shipped. The six files I marked
      superseded did not include the 0.5.0 plan, because that one is *current* — and a current plan
      is exactly where a stale box is most credible.

      **Process change, adopted mid-loop:** before dispatching any agent at a documented task, the
      main loop spends thirty seconds proving the thing is actually absent — a `grep` for the
      symbol, an `ls` for the file, a `git log` for the commit. Cheap disproof first. This is the
      largest single token saving available to this loop, and it is the operator's own efficiency
      ask answered at the level of process rather than model choice.
- [x] **H2 — mark the stale plan files superseded.** Header, not retroactive ticks. Done in
      `4e225f5`. It was **six** plan files, not eight — the survey folded two roadmap-spec items into
      its count — carrying **144** unchecked boxes between them, plus a roadmap tier listing four
      built services as future work. Each header records the evidence checked, so the claim is
      falsifiable rather than trusted.
- [x] **H3 — retention policy for `export_snapshot_runs` and `export_snapshots`.** **Measured, and
      deletion refused.** Spec: `docs/superpowers/specs/2026-08-16-export-archive-retention-design.md`
      (`9151fbd`). It was two tasks with opposite answers. The runs ledger is bounded by its own
      `(run_day, outcome)` unique constraint at four rows a day — a retention policy there would be
      code running forever to delete nothing. The archive is forward-only, so every retention policy
      deletes oldest-first and **oldest-first is the order of irreplaceability**. Measured against
      production: **77** published rows across all eleven datasets, ~3 KB a snapshot, ~1.1 MB a year.
      The reason it is 77 is the finding — 520 meetings ingested, one published, so growth tracks the
      operator's publication rate and not the machine's ingestion rate.
- [x] **H4 — merge the two vote-tally implementations.** Done, `57f8463`. Diffed before merging;
      they agreed exactly, so this was duplication and not a latent under-count. Null result recorded
      rather than left silent.
- [x] **H4b — the drift guard that came out of reviewing H4's test.** `0f00790`. H4's new unit test
      pinned the four `vote_value` members against a literal list in the same file: proof the
      frontend agrees with *itself*. Nothing checked it against the database. A fifth enum member
      passes every check in this repo and renders **NaN** on a public meeting page, with the vote
      missing from a tally still presented as complete. Now read from `pg_enum`.
      **Mutation-verification found a real defect in my own guard**: the rename mutation *passed*,
      because the matcher had no word boundary and matched the name as a prefix. Anchored, re-run,
      fails correctly. It nearly shipped able to pass while blind.
- [x] **H5 — the `argsIgnorePattern` lint decision.** **Moot.** `npm run lint` exits 0 with no
      output in *both* packages. The two warnings STATUS describes are gone; there is no decision
      left to take. Third stale item of the loop.
- [ ] **H6 — MT CERS public route and frontend.** Genuinely unbuilt. Campaign-finance filings name
      candidates, so this ships **behind a feature flag defaulting off** — buildable now, releasable
      only by the operator.
- [ ] **H7 — batch extraction runner.** No queue-stage batch route; only per-meeting.
- [ ] **H8 — repetition-loop truncation.** 20.8% of extraction chunks truncate. Needs a raw-bytes
      probe before any design — measure, do not reason.

### Not for this loop — operator decisions

Recorded so the loop does not quietly reclassify them as buildable. `claim_publication`; the office
gate; enabling `dated_export_archive`; generated finding narrative; the real member roster (blocked —
Bozeman roster 403s from Akamai); `member_id` population (blocked behind the roster); the
published-claim page design; enabling the dark delivery surfaces; geography approvals;
`jurisdiction_records_law` population (statute reading, explicitly non-delegable); `rundown_sheets`
revive-or-retire; the W6 funding network layer. Recordings/transcription stays **refused**.

**The reach items deserve a note.** `EVENT_DRAIN_ENABLED`, MCP and prerender are shipped dark over
content that is *already* publication-approved. It is tempting to read that as "so turning them on
changes nothing." It changes the **reach** — a prerendered page is a file on disk that a crawler
indexes, and MCP hands the material to other software. Approved-for-the-site is not the same consent
as approved-for-everywhere, and that difference is the operator's to weigh.

## Tick log

- **01:35Z** — Loop started. Tree clean at `58a14f1`; `origin/main` and `github/main` both match, so
  nothing was outstanding to commit or push. Dispatched the backlog survey (sonnet, read-only)
  across `docs/`, `CHANGELOG.md`, every plan checkbox, and a TODO/FIXME sweep of `backend/src` and
  `frontend/src`. Rung 1 cannot be declared exhausted until that returns.
- **01:41Z** — Survey returned. Rung 1 has real content, so no rung below it is eligible yet.
  Dispatched H1 (sonnet) on the event-log leaks. Did **H2 in the main loop** while that ran, since it
  is docs-only and disjoint from `backend/test` — six files, 144 boxes, committed as `4e225f5`.
  Verified rather than assumed: the auth files and migrations, migration 024 and its
  `delivery_channels` consumers, migration 035 with `search.ts` mounted, the four Tier-6 service
  paths, and — the one that would have mattered — that `search.test.ts` really does assert the
  unpublished-meeting wall in both directions. Had that assertion been missing, the box would have
  been a live gap and marking the file superseded would have buried it.
- **01:58Z** — H1 came back "already done" — the 0.5.0 plan's boxes were stale, which is H2's own
  hazard catching this loop. Adopted the cheap-disproof rule and immediately applied it to H3–H6:
  three of the four were confirmed genuinely absent, and **H5 was the third stale item of the loop**
  (lint exits 0 with no output in both packages). H3 turned out to be two tasks with opposite
  answers and produced a spec rather than a `DELETE`. H4 merged cleanly with no behavioural
  divergence — and reviewing its test surfaced **H4b**, a real gap where the frontend's vote
  vocabulary was checked only against itself. Mutation-verifying H4b found a defect in H4b.
  Verified in the main loop: backend **2042 / 492**, frontend **740 / 65**, 0 fail, 0 skipped,
  leak check clean, both builds green, lint clean both packages.
