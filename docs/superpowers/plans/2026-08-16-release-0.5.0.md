# Release 0.5.0 — the second autonomous loop

**Started 2026-08-15 21:59Z. Hard deadline 2026-08-16 01:59Z.** Closing tasks Z1–Z3 begin at
**01:20Z**. Run `date -u` to check the clock. **Do not infer the current time from git history** —
the previous loop anchored its deadline to commit timestamps and nearly shut itself down after one
chunk of work.

This file is the loop's durable memory. Every tick: read it, pick the next unfinished task in order,
update the checkboxes and the tick log.

---

> **SUPERSEDED at 22:31Z — the disk was cleared and the backlog deployed.** Deploy run **29057
> completed success** and production serves **`f815d36`**, built 22:15:28Z. `/api/health` 200,
> `/api/admin/features` 401 (mounted and guarded, not 404). Run 29062 is already in flight on
> `e83eb5e`. The section below described the constraint this loop began under; it no longer holds,
> and **production verification is available again** — use it. See tick 4.

## The situation this loop starts in

**Production serves `72c10b0` and the deploy host is out of disk.** Everything committed after that
sha is pushed and not running. Clearing the disk is operator-only — there are no valid AWS
credentials on this machine (`InvalidClientTokenId`). See `docs/STATUS.md` § BLOCKING OPERATOR ACTION.

**What follows from that, and it constrains every task below:** *this loop cannot verify anything in
production.* The previous loop's most valuable finding — that a console toggle reached only one of
six features — came from probing a deployed build, and that check is unavailable. So: prefer work
whose correctness is demonstrable from tests and real command output alone, be more conservative
about what gets built without a live probe, and **state the unverified-in-production status in the
changelog completeness factor** rather than letting it read as shipped.

Pushing to `origin` still fires a deploy that will fail on the same disk. That is harmless — the
pull fails before containers are replaced, and the site keeps serving. It doubles as a probe of
whether the operator has cleared the disk yet. Push anyway; do not treat the red run as this loop's
defect.

---

## Standing rules

1. **One dev subagent at a time.** The main loop plans, verifies, commits, keeps loop health, and
   answers the agent via `SendMessage`. If the agent is still running at a tick, do not dispatch a
   second one — take a main-loop-only task instead, or do nothing.
2. **The subagent never runs a git write command.** No `add`, `commit`, `checkout`, `stash`,
   `reset`, `push`. Read-only git is fine. The main loop commits.
3. **`git add -A` is banned while an agent is running.** It swept 1,083 lines of an in-progress
   feature into an unrelated `docs:` commit last loop. Stage explicit paths.
4. **Verify with real command output, never with an agent's claim.** Backend and frontend
   `typecheck` + `test` + `build` before every commit. **`blocked` is not `pass`.**
5. **Never run the backend suite from the main loop while the agent may be running it** — one test
   database, and concurrent runs collide.
6. **A guard that has never been seen to fail is not known to work.** Every new invariant test gets
   mutation-verified in both directions, and the mutation output goes in the commit message.
7. **Push to `origin` (Gitea), not `github` (the mirror).** Read the printed sha range, not the exit
   code.
8. **Nothing new publishes prose about a named person without a flag defaulting to off and the
   review queue in front of it.** The walls get no flag — see the feature-registry design.

---

## Tasks, in order

### G1 — the test suite's own reliability  *(small, first, unblocks trust in every later gate)*

- [x] **G1a** `backend/test/prerender.test.ts` drives `consumer.tick()` from a **null cursor**, and
      `tick` reads `limit(200)` in `(updated_at, id)` order. Once `events` holds >200 rows older than
      the fixtures, the tick spends its batch elsewhere and the suite fails — **including on "a
      withdrawn meeting kept its prerendered page"**, the worst failure this system can produce.
      Seed a cursor positioned just before the fixtures instead of starting from null.
      **Acceptance:** the test fails on `main` with ≥201 orphaned older events present, and passes
      with the fix under the same conditions. Demonstrate both.
      *(Was F9b.)*

### G2 — the silent deploy failure  *(the finding the last loop ended on)*

- [x] **G2a** Nothing alerted when the release stopped deploying, because the external monitor asks
      whether the **site** is up and it is. Add a **release-drift check**: compare
      `/api/version`'s sha against the head of `origin/main`, and report a drift older than a
      threshold as a failure. This needs no AWS credentials and no host access.
      **Where:** extend the existing monitor rather than adding a second one — see
      `deploy/monitor-trigger.sh` and the monitor service in `backend/src/services/`. Read the
      existing check shape first; do not invent a parallel mechanism.
      **Acceptance:** a test in which the reported sha differs from the expected head produces a
      failing check with both shas in the message, and one in which they match produces a pass.
      **Guard:** the check must distinguish *"the shas differ"* from *"the version endpoint could not
      be read"*. Those are different events and reporting them the same way is the exact error
      pattern this project keeps finding.

- [x] **G2b** Two gaps found reviewing G2a, dispatched immediately after it.
      **The settle race:** `deploy.yml` runs the drift check at allowance `0`, and the health step
      before it exits on the *first* 200 — which can come from the old container mid-swap. A good
      deploy could read a stale sha and turn the pipeline red. Raising the threshold cannot fix it:
      drift age is measured from **commit** time, already ~12 min old when `deploy-aws` finishes, so
      any threshold below that still fails an in-flight swap. Needs a bounded re-read.
      **The unenforced variable:** drop `MONITOR_EXPECTED_SHA` from a workflow and the check reports
      BLOCKED while the run stays green — correct behaviour (blocked must not page) that also means
      an unrelated YAML edit can silently disable the check. Needs a test reading the real workflow
      files, asserting both variables reach the container in `env:` *and* in the `docker run -e`
      list, since one without the other is the same silent failure.

- [x] **G2d** *(filed by G2b, done in tick 13)* **`evaluateVersion` has the same race and no settle.**
      The `/api/version` vs `/version.json` skew check is judged from a single first read, so a swap
      caught with one image rolled and the other not turns `deploy.yml` red with a convincing
      "half-finished rollout" message. Lower probability than the drift case — both are read
      milliseconds apart, and `deploy-aws-ssm.sh` asserts host-side agreement before exiting — but
      it is the same defect.

### G2c — verify 0.4.0 in production, now that it is running  *(main loop, read-only)*

- [ ] **G2c** Everything committed after `72c10b0` reached production only at 22:15Z and **has never
      been observed running**: the extraction de-duplication, the `repetition-truncated` reason, the
      dated export archive, the `ALERT_FROM_EMAIL` fix, the DataLicensePage pin. The last loop's
      changelog calls these *shipped dark, unverified in production* and that wording is now
      falsifiable. Probe what can be probed from outside and correct the completeness factors in Z1
      to say what was actually seen.
      *First reading, 22:31Z:* `/api/data/archive` → **404**, which is consistent with **both** "the
      flag is off" and "the route is not mounted", so it does not distinguish them — the same class
      of ambiguity this project keeps finding. Do not record it as either without a second signal.

### G3 — the archive with no data in it

- [x] **G3a** `dated_export_archive` shipped with a writer (`npm run export:snapshot`) that **nothing
      ever calls**, so the feature serves 404 for every date. Give it a scheduled path: a snapshot
      stage that runs on the existing Postgres `SKIP LOCKED` queue / scheduler rather than a new
      timer, gated behind the existing `dated_export_archive` flag, **default off**.
      **Acceptance:** with the flag off, no snapshot row is written and the scheduler records that
      it was skipped rather than silently doing nothing. With it on, one run writes exactly one
      snapshot per day and a second run the same day is a no-op rather than a duplicate.
      **Read first:** `backend/src/services/export/archive.ts`, `backend/src/scripts/export-snapshot.ts`,
      and the ingestion scheduler — reuse its idioms.

- [x] **G3b** *(filed by G3a, dispatched immediately)* The ledger is served on
      `GET /api/admin/features` and **nothing renders it**. The whole reason it exists is that an
      operator who turns the key on and sees no snapshot must be able to tell "skipped every cycle,
      flag off" from "not running at all", and `/api/data/archive` 404s while the flag is off so it
      cannot report on its own scheduler. **A disclosure nobody can read is not a disclosure.**
      The empty case is the load-bearing one: *never on* / *nothing recorded yet* / *the request
      failed* / *the backend never sent the field* must read differently.

- [x] **G3c** *(filed by G3a, not scheduled)* `npm run export:snapshot` can add a second snapshot on
      a day the scheduler already recorded. Harmless — the archive resolves a date to the latest
      snapshot on or before it — but a manual run silently supersedes the scheduled one for that day.
- [x] **G3e** *(filed by G3b, done in tick 13)* `SNAPSHOT_RUN_DAYS = 30` is applied as `.limit(30)` on
      **rows**, not days, and a day can yield up to five rows (one per outcome). The served window is
      somewhere between six and thirty days while the constant's name and its doc comment both say
      "the last 30 days". The console deliberately does **not** print "the last 30 days" for that
      reason. Fix by bounding on `run_day >= now() - 30 days`, which is what the comment promises.
- [ ] **G3d** *(filed by G3a, not scheduled)* No retention policy on `export_snapshot_runs` or
      `export_snapshots`. Bounded at a few rows per day so nothing is urgent, but migration 105
      already flags that `row_ids` as jsonb does not scale past this corpus.

### G4 — configuration that is still hardcoded  *(first to drop if the clock forces it)*

- [ ] **G4a** Move the hardcoded body lists into `ingestion_sources.config`. A new body currently
      needs a code change and a deploy, which on a host that cannot deploy means it needs an
      operator with AWS access.
      **Acceptance:** the existing adapters resolve their bodies from the source row, the current
      production values are preserved by a migration rather than by a default in code, and a
      contract test asserts that a source row with no `config` behaves exactly as today.
      *(Was F8.)*

### G5 — **REFRAMED at 23:10Z: two of the six switches control nothing**

The plan assumed G5/G6 meant *building* generated narrative and claim publication behind keys that
were waiting for them. That is not the situation. Grepped every consumer of the registry:

```
routes/mcp.ts:90                    resolveFeature("mcp_server")
services/export/snapshot-scheduler   featureEnabled("dated_export_archive")
services/prerender/consumer.ts:132   featureEnabled("prerender")
services/events/drain.ts:105         featureEnabled("event_drain")
routes/data.ts:124                   featureEnabled("dated_export_archive")
```

**`claim_publication` and `generated_narrative` appear nowhere outside `manifest.ts`.** The console
renders two switches that are wired to nothing, and their descriptions state otherwise:

- `claim_publication` says it decides "whether approved claims are shown". **Approved claims are
  already shown** — `whereClaimPublic` + `renderApprovedClaim` serve them through `feeds/entries.ts`,
  `export/datasets.ts` and `public-corrections.ts`. All three are correctly walled and **none** is
  gated. An operator turning this off would reasonably believe published claims stopped appearing.
  They would not.
- `generated_narrative` describes "the findings composer" drafting prose into the review queue.
  **There is no composer.** The console advertises a capability the build does not contain.

This is the 0.4.0 defect again — a switch that reaches nothing — but worse, because F1j's version was
found by building the console, and this one survived it. It is also the project's own failure mode:
**a control that accepts a click and changes nothing.**

**Why the obvious fix is wrong.** Wiring `claim_publication` as a gate that defaults off would
retract claims that are public in production right now, on the next deploy, silently. It would also
break the property 0.4.0 was built on — with no registry row, behaviour is byte-identical — and that
property is what keeps the existing suites honest. A key that defaults *on* would break the other
invariant, that resolution falls off to **off** in every failure mode. Neither is a change to make
at 23:10 with no operator reachable.

- [x] **G5a** Build the guard that would have caught this: **every key in `FEATURES` must have a
      consumer outside `manifest.ts`.** A key with no reader is a switch that lies about what it
      controls. Mutation-verify it in both directions — a key added with no consumer fails by name,
      and a consumer removed from a wired key fails by name.
- [x] **G5b** Resolve the two failures the guard finds **by the honest route, not by wiring**:
      remove the two unwired keys from the shipped manifest so the console shows only switches that
      do something, and record in the manifest why they are absent and what has to exist before
      either returns. Do **not** wire `claim_publication` to the public claim surfaces in this loop —
      that is a live-content change and it needs the operator.
- [x] **G5c** Write `docs/superpowers/specs/2026-08-16-claim-publication-gate-design.md`: what
      gating already-public claims should do on first deploy, whether a `publishes` key may ever
      default on, and how an operator is told the difference. Spec only — no implementation.

### G5-OLD — generated narrative, built dark  *(superseded by the finding above)*

The operator said: build the backlog, and gate anything that should not be released behind the admin
panel. `generated_narrative` and `claim_publication` are **already keys in the manifest, defaulting
to off**. So these get built — dark, flagged, and behind the review queue — not skipped.

**But the order matters and G5 comes before G6:** narrative is generated *into the review queue*,
where a human sees it before anyone else does. Publication is the step that removes the human. Build
the reviewable thing first.

- [ ] **G5a** Read `docs/superpowers/specs/` for any existing narrative design and
      `backend/src/services/review/` for the queue's contract. **Write a short spec first** —
      `docs/superpowers/specs/2026-08-16-generated-narrative-design.md`. It must state: what the
      generator may say, what it may not, and how a claim traces to a stored artifact with a locator.
      **Describe the record, never the motive.** No assertion of intent, corruption or illegality.
- [ ] **G5b** Implement the generator behind `generated_narrative`, writing **drafts to the operator
      review queue only**. Never to a published surface, on any code path.
- [ ] **G5c** Invariant tests, mutation-verified: a generated draft cannot reach a published state
      without an operator transition; a draft containing an unsourced assertion is refused at
      persistence; the flag defaulting to off means the generator does not run.

### G6 — the claim publication path  *(only if G5 is complete and verified, and only before 00:45Z)*

- [ ] **G6a** The path from an approved claim to a public surface, behind `claim_publication`,
      default off. **Do not start this after 00:45Z** — a half-built publication path is the one
      unfinished state in this repository that could put unreviewed text about a named person on a
      public page. If the clock beats it, leave G6 unstarted and say so in the changelog. An
      unstarted feature is a known gap; a half-finished one is a defect that reads as a feature.

### G8 — test-suite soil  *(filed by G1a, take only if the queue empties)*

- [ ] **G8a** Several suites emit events and never tear them down; the test DB carries 155 orphans
      and it regrows. A per-suite teardown, or a test-only `pretest` truncate. **Migration 083 is
      "Retention: never delete"** — any truncate must be unmistakably test-environment-only.
- [ ] **G8b** Two suites hand-roll the same cursor-seeding block. Consolidate into `test/helpers/`.

### G7 — the office gate  *(spec only, do not implement)*

- [x] **G7a** 283 of 336 extraction rejections are `not-an-official` — a Weed District board printing
      bare first names against a prompt demanding Mayor/Commissioner. Loosening the gate is the
      change that puts an unverifiable person's name on a page, so **this loop writes the spec and
      stops**: what a sourced roster is, where it comes from, and what the gate checks against it.
      No implementation without an operator.

---

## Closing tasks — begin at 01:20Z

- [x] **Z1** `CHANGELOG.md` — a `0.5.0` section with a **completeness factor on every feature**.
      Anything built this loop is at best *shipped dark, unverified in production* — the disk is
      still full. Do not write "shipped" for something no probe has seen run.
- [x] **Z2** `docs/STATUS.md` — what is now true, the new defects, and whether the blocking operator
      action is still blocking. **Re-check the deployed sha before writing it.**
- [ ] **Z3** Verify the deployed sha and the last deploy run's conclusion. Record the result whether
      or not it changed. Then `CronDelete` the loop job and post the final summary.

---

## Tick log

Append one line per tick: time, what the agent was given, what landed. Terse.

- **21:59Z — tick 0.** Loop armed (cron `*/15 * * * *`, job `176a33ea`). Clock read from `date -u`,
  not from git. Production confirmed still on `72c10b0`, `/api/health` 200 — the disk is not yet
  cleared. Roadmap written and committed `b96446e`. Agent dispatched on G1a.
- **22:09Z — tick 1.** **G1a landed** (`bbd3196`), both demonstrations real: unmodified file fails
  3/11 under 530 old renderable events, failing precisely on the withdrawal assertion; fixed file
  passes 11/11 under the same conditions. Verified independently in the main loop — 1952 pass, 0
  fail, 0 skipped, typecheck and build clean. No production code touched.
  *Threshold correction:* it is 201 **renderable** rows, not 201 rows — `tick` filters
  `whereIn(subject_kind, RENDERABLE_KINDS)` and the existing orphans are mostly `dispute`. A first
  attempt with 250 injected rows did not reproduce the failure.
  *Filed, not fixed:* (a) the test DB still holds 155 orphaned events because several suites emit
  and never tear down — the same soil the 1,585-row incident grew in, and it will regrow; any
  truncate must be explicitly test-only, since migration 083 is "never delete". (b) two suites now
  hand-roll the identical cursor-seeding block; a `test/helpers/` function is the natural
  consolidation. Both are **G8**.
  *Noted, accepted:* the seeding is its own regression guard, but there is **no test that fails if
  the seeding is removed** — proving it needs 201 injected rows and that cost is not worth it inside
  the suite. The commit message carries the demonstration instead.
- **22:16Z — tick 2.** Agent running on G2a (it found and is extending
  `backend/src/scripts/external-monitor.ts` rather than building a parallel mechanism), so no second
  dispatch and **no backend suite from the main loop** — one test database. Did the read-only probe
  instead, which turned up the evidence for G2a's own premise:

  ```
  29058 monitor.yml  completed success   f815d36d
  29057 deploy.yml   in_progress         f815d36d
  29053 monitor.yml  completed success   c5938ca3
  29049 monitor.yml  completed success   c5938ca3
  ```

  **The monitor has reported success every 15 minutes across the entire window in which the release
  stopped landing**, while `/api/version` still answers `72c10b0` from 18:24:55Z. So the gap is not
  that the monitor is silent about drift — it is that the monitor emits a **green** signal while
  drift exists, which is the signal an operator would cite to conclude nothing is wrong. Sent to the
  agent as design input, with the caution to construct both states in tests rather than read live
  state: run 29057 is in flight on head, and if it succeeds the drift closes on its own.
  224 min to closing.
- **22:36Z — tick 3.** **G2a landed** (`7007de7`), verified independently: 1971 pass (+19), 0 fail,
  0 skipped, typecheck and build clean. It extends the existing `external-monitor.ts` in the same
  `CheckOutcome` shape rather than adding a second mechanism, and the load-bearing part is the new
  fourth `CheckState`, `blocked`: an unreadable `/api/version`, a payload with no sha, unparseable
  JSON, an unstamped `sha: "unknown"`, a missing expected head and a garbage threshold **all report
  BLOCKED, never PASS and never the drift wording**. The expected head comes from the runner, never
  from anything the site serves — the self-comparison that would pass forever. Threshold measured
  from three real green pipelines (11.3 / 11.7 / 12.5 min), not guessed.
  *Reviewing the diff found a race the tests could not:* see **G2b**. Dispatched straight away.
  *Commit-message trap:* backticks in a `-m` string are command substitution and silently ate two
  phrases. Amended via `-F` from a file. Worth remembering — the commit succeeded, exit 0, with
  words missing.
- **22:31Z — tick 4. The blocking operator action is resolved.** Deploy **29057 completed success**;
  production serves `f815d36` built 22:15:28Z, `/api/health` 200, `/api/admin/features` 401. The
  0.4.0 backlog that had been committed-and-not-running since 18:24Z is now running, and run 29062
  is already in flight on `e83eb5e`. **The constraint this loop was planned under is gone** — the
  header is marked superseded rather than edited away, and **G2c** is filed to verify what has never
  been observed running. Agent still on G2b, so no dispatch; this was main-loop, read-only work.
  *Timestamp correction:* the tick-3 entry is labelled 22:36Z and the real time was ~22:30Z — I
  wrote a guessed clock into a durable record in the same file that opens by telling the reader not
  to infer time. Left in place with this note rather than silently rewritten.
  208 min to closing.
- **22:47Z — tick 5.** **G2b landed** (`632672b`), verified: 1992 pass (+21), 0 fail, 0 skipped,
  typecheck, lint and build clean; `deploy.yml` confirmed **comment-only** by filtering the diff.
  The settle is 3 attempts 10s apart, and the number is not invented — `deploy-aws-ssm.sh` already
  gives the swap 10 polls × 3s = 30s to become visible before it exits 0, so this matches from
  outside the box what the host allows inside it. **A match costs zero re-reads**, so the periodic
  run is unchanged; the 30s is spent only when the sha already disagrees. Both guards
  mutation-verified — disabling the settle fails 6 of 58 tests reproducing the bug in its own words
  ("a finished swap must not fail the deploy"), and deleting one `env:` line plus one `-e` line
  fails exactly the two tests that name them. The agent added the new test file to
  `package.json`'s explicit list unprompted, which is the trap that would otherwise have made the
  guard never run. Filed **G2d**. Dispatched G3a.
- **22:46Z — tick 6.** Agent on G3a, no second dispatch. Checked its in-progress diff for the one
  instruction most likely to be broken — **it did not add a feature key**; it extended the existing
  `dated_export_archive` description and added the loop's interval to the console's
  `cycleIntervalMs`. Correct.

  **G2a verified in production, by its own output.** Monitor run 29063 on `e83eb5e`, job 39292:

  ```
  PASS  health: 200, database connected
  PASS  version: both images serve f815d36
  WARN  release-drift: live f815d36 (f815d36d…) vs expected head e83eb5e (e83eb5e2…),
        committed 1.8 min ago — within the 30-min allowance, so a deploy is probably still in flight
  No check failed (2 warning(s), 0 blocked — blocked means the answer could not be
  determined, not that it was fine).
  ```

  It reached the real runner, resolved a real expected head, named **both** shas, aged the drift
  correctly and chose WARN over FAIL because a deploy genuinely was in flight. The summary line
  carries the blocked count with its meaning attached. This is the check that did not exist during
  the four-hour outage this morning, answering correctly on its first production run.
  154 min to closing.
- **23:05Z — tick 7.** **G3a landed** (`66d1f54`), verified both packages: backend 2008 pass (+16),
  0 fail, 0 skipped; frontend 64 files / 728 pass; typecheck, lint and build clean in both;
  migration 106 as batch 45.

  Hooked as an in-process loop beside the other two, **not** on the ingestion queue — a snapshot is
  neither per-document work nor an ingestion source, and hanging it there would have meant inventing
  a fake `ingestion_sources` row, i.e. a non-source in the table the **public status page** reads.
  Hourly rather than a daily cron, because a daily cron fires at one instant and a container
  restarting at that instant loses the day permanently; hourly polling plus a same-day guard makes
  "one per UTC day" a property of the data rather than of the timer.

  The load-bearing part is that **a skipped cycle is written down**. The silent-skip mutation is the
  one worth remembering: snapshot behaviour is byte-identical and only the disclosure disappears, so
  without that test the feature would look correct while going dark. It fails 4 tests, including
  *"today's skipped cycle is not on the console"*.

  **Ordering decision, made here:** G3b is dispatched ahead of G4. G4 is a refactor with modest
  value; G3b finishes an operator story that is currently half-built, and the operator console is
  the thing this whole session was asked for. If the clock forces a choice, **G4 is the one to
  drop**, not G5. Filed **G3c** and **G3d**.
- **23:16Z — tick 10.** **G3b landed** (`1418b9d`): frontend 737 pass (+9), typecheck and build
  clean, no backend file touched. The empty case turned out to have **four** meanings and each got
  its own sentence; the mutation collapsing *absent* into *empty* fails by name, with the DOM dump
  showing the page claiming "nothing has ever turned `dated_export_archive` on here" against a
  response that never carried the field. Filed **G3e** — `.limit(30)` on rows, described as 30 days;
  the agent noticed and refused to print "the last 30 days" on the console.

  Agent on G5, so no dispatch. **Wrote G5c in the main loop**:
  `docs/superpowers/specs/2026-08-16-claim-publication-gate-design.md`. It maps the three ungated
  surfaces to their routes (`feed.ts`, `data.ts`, `corrections.ts`), states why wiring the gate is a
  live-content change rather than a wiring fix, and lays out three options with their real costs —
  A: no switch at all, consistent with the review path's refusal to have a bulk approve; B: default
  on, which breaks *falls off to off* and makes an unreachable database publish; C: default off plus
  a migration writing the row, which keeps the resolution rule but opens the audit log with a change
  nobody made. **C is what I would pick if forced, and it is still the operator's call.**
  One constraint the spec adds that none of the options may break: **a person's correction must not
  be hideable.** Gating `corrections.ts` would let an operator make a correction about a named person
  invisible while the original claim stayed cited on two other surfaces.
  124 min to closing.
- **23:26Z — tick 11.** **G5a and G5b landed** (`5552f40`): backend 2010 pass, frontend 737 pass,
  both typecheck and build clean, compose still validates.

  **The guard reproduced the defect unprompted on its first run**, failing with both key names
  before either was removed. Mutation-verified three ways, and the third is the one that matters:
  breaking the matcher so it finds nothing fails **loudly** — *"the scan cannot find `event_drain` in
  the manifest that declares it — the matcher is broken"* — rather than comparing two empty sets and
  passing. This repository has been bitten by that exact vacuity before.

  **I was wrong about the blast radius and the agent corrected me.** Approved claims are public
  through **six** surfaces, not three: the three I found, plus `listPublicClaims` via
  `GET /api/meetings/:id/claims`, which `prerender/pages.ts` and `delivery/mcp.ts` reuse whole.
  Verified before accepting it. The spec is corrected in place with the count and the correction
  noted, because the two extra surfaces change the design question: **a prerendered page is a file
  already written to disk**, so a flag going off does not unwrite it — a switch over claims would
  have to enqueue a re-render of every affected meeting to take effect at all.

  **Worth remembering: the type system did not catch the removal.** `FeatureKey` is a literal union,
  but every reference to the removed keys was a runtime string — supertest path params and
  `findFeatureDefinition(key: string)` — so backend typecheck passed with the keys gone and the tests
  still naming them. The runtime tests caught it. A literal-union key type only protects call sites
  that use the type.

  Dispatched G2d + G3e together: both small, disjoint files, and G2d now matters more than it did an
  hour ago because deploys are landing again. **G4 remains the one to drop.** 114 min to closing.
- **23:31Z — tick 12.** Agent on G2d/G3e, no dispatch. **Drafted Z1 in the scratchpad**, not in the
  repo, so it cannot mix into the tree the agent is editing —
  `scratchpad/changelog-0.5.0-draft.md`. Every section carries its completeness factor, and the two
  that are not "Shipped" say why: the dated archive is **shipped dark** (the loop is live, the flag
  is off, so it writes a skip row every cycle and no snapshots), and the drift check is the only one
  claiming **verified in production**, because monitor 29063 is a real observation rather than a
  test result. The G2d/G3e section is left out entirely rather than written speculatively — the same
  rule as last loop's F2 hole. 109 min to closing.
- **23:45Z — tick 13/14.** **G2d + G3e landed** (`26359bc`), and the two copy corrections they made
  necessary (`91ace3e`). Backend 2027 pass (+17), frontend 737, all gates clean.

  The settle was **extracted** into a generic `settle()` rather than copied — and the evidence it did
  not change the check it was lifted from is that **every drift message string is byte-identical and
  the pre-existing settle tests pass untouched.** One deliberate divergence between the two checks:
  a garbage settle config leaves an observed skew as **fail**, not `blocked`, because the skew *was*
  read and a typo in an env var must not be able to silence a half-rolled stack.

  The mutation that matters is the second direction — making the settle **too generous** fails 5,
  including *both* checks' persistence guards. That is the pair that stops a settle laundering a real
  skew.

  G3e went further than scoped: the row cap survives as `days × outcomes`, the **arithmetic ceiling**
  of the window guaranteed by the unique index on `(run_day, outcome)`, so it can never shorten the
  window again. The signature moved from a positional `limit` to an options object on purpose — a
  positional `30` silently changing meaning from rows to days is the same defect being fixed.

  Then two hedges that were true this morning and are not now: the console heading, and `deploy.yml`
  describing the settle as drift-only. Corrected, with the old reasoning kept as the record of why
  the hedge existed.

  **Dispatched G8 over G4.** G4 enables adding a body without a deploy and there is no body waiting
  to be added; G8 protects the test suite every future session depends on, and it is the *cause*
  behind the symptom `bbd3196` fixed. **G4 is now formally dropped from this loop.** 95 min to
  closing.
- **00:01Z — tick 15.** Agent 14m into G8 (building `test/event-log-hygiene.test.ts` plus
  `helpers/events.ts` and `helpers/reset-events.ts` — a guard, not just a cleanup, which is the right
  shape). No dispatch. **Wrote G7a in the main loop:**
  `docs/superpowers/specs/2026-08-16-office-gate-design.md`.

  The spec's load-bearing finding is one I did not expect and would have got wrong by reasoning:
  **sourcing the roster does not unlock the 283.** A sourced roster makes "Commissioner Bode"
  checkable; it does not make "Dave" checkable, and the 283 are bare first names with no office at
  all. So Option B is not "the right answer, later" — it is a different answer to a different
  question, and a future reader in a hurry would have merged them.

  Recommendation: **A + C together** — exclude the body from extraction at the source, and surface
  the per-body breakdown so the rejections are self-explaining. Neither touches what is published;
  they change what we *spend* and what we *show an operator*. B becomes its own project starting with
  a probe of real roster pages, per the project's own rule about probing before designing.
  One constraint recorded: `RECORDED_OFFICES` must not grow as a way to admit bare names, and the
  F2d loss measurement must be re-run after any change here, because changing what passes the gate
  changes that measurement's denominator. 79 min to closing.
- **00:17Z — tick 17.** **Z1 and Z2 written early** (`66857bb`), deliberately ahead of the closing
  window so the last dev task is not racing the documentation. STATUS.md leads with the **three
  operator decisions this release created, none of them taken autonomously**. The dropped G4 is
  named as a decision rather than left as an absence.
- **00:27Z — tick 18.** **G3c landed** (`f244fef`): 2038 pass (+5), all gates clean, posttest
  event-log check clean. It chose **refuse by default, `--force` supersedes and moves the ledger** —
  and the reasoning is the keeper: *refusal is the only outcome that is cheap when wrong.* A retyped
  flag costs a second; a supersession cannot be undone by taking another snapshot, and a printed
  warning arrives **after** the day's meaning has already changed. Two fixes fell out that I had not
  scoped: a manual run on an *empty* day now claims the day in the ledger too (otherwise the
  scheduler's next cycle filed a lone `skipped_same_day` against a snapshot no row claimed — the same
  disagreement in the other direction), and the manual path takes the scheduler's advisory lock so a
  manual run and a tick cannot both pass the same-day check.
  Both packages bumped to **0.5.0** (`b348aa4`) and pushed to both remotes. Z3 polling.
