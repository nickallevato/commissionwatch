# Plan — release 0.4.0

**Started:** 2026-08-15, autonomous 4-hour loop, 15-minute ticks.
**Operator is away.** No question blocks work: state an assumption in the commit body and proceed.
**Spec of record for F1:** `docs/superpowers/specs/2026-08-15-feature-registry-design.md`

## How this file is used

This is the loop's memory. The orchestrator reads it at the top of every tick and the dev subagent
is pointed at exactly one task from it. Update the checkbox **and** the notes line the moment a task
lands — a tick that loses context must be able to resume from this file alone.

States: `[ ]` not started · `[~]` in progress · `[x]` landed and committed · `[!]` blocked, reason
stated · `[-]` dropped, reason stated.

## Standing rules for every task

- `cd backend && npm run typecheck && npm test && npm run build`, and the frontend equivalent, before
  any commit. Backend tests need `docker compose up -d db`.
- **Register every new test file in `backend/package.json`'s explicit test list.** The list is
  explicit; a new file not added to it runs nowhere and reads as passing. This has bitten before.
- No `any`, no `@ts-ignore`, no cast to quiet the compiler, no skipped or deleted test.
- The database schema is the source of truth for types.
- Never add `.github/workflows/`. CI is Gitea Actions.
- New migrations append: next free number is **104**. Do not fill the gaps at 096 and 101 — they are
  archive-salvage artifacts and reusing them makes two lineages collide.
- No CHECK constraint spanning a nullable column. See `098_null_safe_check_constraints.ts`.
- Any feature that changes what a stranger can read ships **behind a registry flag, default off**.
- The dev subagent does not run `git add`/`commit`/`checkout`/`stash`/`reset`. The orchestrator
  commits. Read-only git is fine.

---

## F1 — Feature registry and the admin console  ⟵ START HERE, everything else depends on it

**F1a–F1c landed.** 1869/1869 backend tests pass, typecheck and build clean. Decisions the spec did
not settle, recorded here because F1d–F1i depend on them:
- `mcp_server` is `risk: 'publishes'`, not `low`. It exposes no unpublished record, only a new
  audience — but reachability is what a risk grade is for, and the conservative grade costs nothing.
- **Writing `false` where no row exists is not a no-op.** Off-by-decision and off-by-default are
  different facts, and the former is the write that overrides a legacy env var still saying on.
  `enabled_from` is null there, which is what the nullable column is for.
- The pre-load gate is *"is this key in the cache"*, not `loadedAt !== null` — a key is in the map
  only if this process read it from the database, so a post-commit `setFlag` reflects immediately
  without a second flag.
- `EVENT_DRAIN_ENABLED=false` reports `source: 'default'`, not `'legacy-env'`. Same value, and
  honest: nothing enabled it.
- The process singleton is **settable, not constructed in the service** — importing `config/database`
  there would open a pool on any flag check, including in tests with no database. `src/index.ts`
  installs it, which is F1d/F1e work.

- [x] **F1a** Migration `104_create_features.ts` — `features` and `features_audit` per the spec's
      schema section. No CHECK over a nullable column.
- [x] **F1b** `backend/src/services/features/manifest.ts` — the compiled `FEATURES` manifest: key,
      title, description, `risk`, `legacyEnv`, and any `requiresSeed` note. Six entries per the spec.
- [x] **F1c** `backend/src/services/features/registry.ts` — `FeatureRegistry` with the four-step
      resolution order, the sync cached `get(key)`, `resolve(key)` returning
      `{ enabled, source, loadedAt }`, the poller, and `setFlag(key, enabled, operatorId, reason)`
      writing both tables in **one transaction**. Reject a no-op write. Reject an unknown key.
      Reject an empty reason.
- [ ] **F1d** Repoint the three existing flags at the registry, preserving today's behaviour exactly
      when no row exists: `eventDrainEnabled` in `services/events/drain.ts`, `prerenderEnabled` in
      `services/prerender/consumer.ts`, `mcpEnabled` in `routes/mcp.ts`. The existing suites for all
      three must pass **unmodified** — if one needs editing, the compatibility is wrong, not the test.
- [ ] **F1e** `backend/src/routes/admin/features.ts` + mount in `routes/admin/index.ts`.
      `GET` lists resolved state per manifest entry; `PUT /:key` writes. Behind `requireOperator`.
- [ ] **F1f** `backend/test/features.test.ts` — resolution order incl. kill-switch precedence,
      unknown-key inertness, no-op rejection, reason requirement, audit row contents, cache-not-loaded
      falling back to off, and the transaction rolling both tables back together.
- [ ] **F1g** `backend/test/feature-registry-audit.test.ts` — **the wall guard.** Assert the manifest
      contains no key that gates a wall: no key matching publication/review/approval/wall/consent
      vocabulary, and assert `publication.ts`, the review gate and the claim wall contain no call to
      the registry at all. Verify by mutation: adding such a key must fail this test.
- [ ] **F1h** `frontend/src/pages/AdminFeaturesPage.tsx` + `.test.tsx` + route in `App.tsx` + link in
      the admin nav. Group by risk; show the deciding source; disable the control when a kill switch
      forces the value, with the reason visible; typed confirmation for `risk: 'sends'`.
- [ ] **F1i** `deploy/.env.example` and `deploy/docker-compose.shared.yml` — document
      `FEATURE_<KEY>=false` as the kill switch and `FEATURE_POLL_INTERVAL_MS`. Do not remove the
      three legacy vars; they still work and the operator steps in STATUS.md reference them.

**Notes:** —

---

## F2 — The extraction repetition loop

The highest-leverage build in the backlog. 20.8% of chunks unread, **100% `truncated-reply`**, and
the cause is legible in yesterday's numbers: truncating documents emitted 86–127 "claims" from
3.5k–6.5k characters of a record containing a handful of votes, with rejection tallies
`unknown-action: 103` and `not-an-official: 113`. That is a repetition loop. Chunk splitting was
correctly refused — splitting a chunk that loops gives two chunks that loop.

- [ ] **F2a** Reproduce it. `npm run extraction:distribution` to re-read the ledger, then run the
      real extractor over one known-truncating stored document and **capture the raw reply**. Do not
      design against the tally; read the bytes and find out what the repetition actually looks like.
- [ ] **F2b** Write the finding into the plan notes below before changing anything. If the repetition
      is the model restating one vote in n phrasings, the fix is a stop condition and de-duplication.
      If it is the model enumerating every name in a roster block, the fix is upstream in the prompt's
      framing of the chunk. **These have different fixes and the bytes decide which.**
- [ ] **F2c** Implement the fix. Candidates, in the order the evidence should be tested against:
      an early-stop on repeated claim signatures; de-duplication by `(action, official, locator)`
      before the verifier ever sees them; a prompt that names the expected order of magnitude. Not a
      token-ceiling raise — it has already been raised twice on the other reading and the error text
      no longer advises it.
- [ ] **F2d** Re-measure over the same corpus and record the new distribution. A fix that does not
      move the 20.8% is not a fix, and reporting it as one is the failure this project refuses.
- [ ] **F2e** If and only if the measured unread share is materially down, make extraction
      schedulable as a queue stage behind flag `scheduled_extraction` (add to the manifest,
      default off). A batch runner is a queue stage, not a loop in a route.

**Notes:** —

---

## F3 — Correct the stale next-step in STATUS.md

- [ ] **F3a** `docs/STATUS.md` next-step **1b** targets `readMessageText` returning no content —
      the branch that fired **zero** times in yesterday's measurement. It reads as the top extraction
      task and would send the next reader down a path the data already closed. Rewrite it to point at
      the truncation/repetition finding, and cross-reference F2.

**Notes:** —

---

## F4 — `ALERT_FROM_EMAIL` default

- [ ] **F4a** The default is `alerts@commissionwatch.org`, which is not the deployed domain
      (`commissionwatch.bmux.sh`), so DMARC alignment fails by construction. Fix the default and add a
      test asserting the default aligns with `PUBLIC_BASE_URL`'s host when both are set. SPF/DKIM/DMARC
      records themselves are an operator/DNS task and stay in STATUS.md as such.

**Notes:** —

---

## F5 — The claim publication path

Behind flag `claim_publication`, default off. The highest-stakes thing this project would publish.

- [ ] **F5a** Spec first: `docs/superpowers/specs/2026-08-15-claim-publication-design.md`. What a
      published claim looks like, where it appears, what it cites, and what withdrawing it does. The
      pinned-bytes guarantee already exists (`rendered_text`, `render_sha256`, `render_version`,
      `approved_by`) — this path must serve **those bytes and not a re-render**.
- [ ] **F5b** Implement behind the flag. A claim never gets its own page; it appears at `#claim-{id}`
      inside the meeting it came from. That decision is already made and holds.
- [ ] **F5c** Retraction reaches the reader. `retractSubject` exists; assert an unpublished claim
      disappears from the meeting page and from the prerendered copy.

**Notes:** —

---

## F6 — Generated narrative for findings (W3 remainder)

Behind flag `generated_narrative`, default off. **On means drafts reach the operator queue, never
the public.** A finding today is the detector's own sentence; the queue, threshold, citation binding
and audit trail are all already built to receive composed prose.

- [ ] **F6a** Spec. Describe the record, never the motive — the composer states what happened, when,
      and in what order, and asserts no intent, corruption or illegality. That is testable and gets a
      test with a vocabulary list, following `vocabulary.test.tsx`.
- [ ] **F6b** Implement as a drafter writing to the review queue. Every sentence carries its citation
      into the draft; a sentence whose source cannot be located is not emitted.
- [ ] **F6c** Tests: no unsourced sentence, no motive vocabulary, nothing auto-published, and the
      flag off means nothing is drafted at all.

**Notes:** —

---

## F7 — Dated export archive

- [ ] **F7a** "What did this site say in March" is currently unanswerable, and `/data` says so rather
      than implying otherwise. Behind flag `dated_export_archive`. Point-in-time exports, addressed by
      date, built from the same walled dataset builders as `/api/data` so there is exactly one
      publication wall in the export path.

**Notes:** —

---

## F8 — Body lists into `ingestion_sources.config`

- [ ] **F8a** Architecture, not a bug. Adapter construction is synchronous and database-free,
      `config.bodies` is slug-only and lossy for both adapters, and `registerSource` never updates an
      existing row. Read the declined list under Known defects in STATUS.md before starting — the
      five skipped Bozeman meetings are **no longer** the reason to do this.

**Notes:** —

---

## Closing tasks — run these when the loop's time is nearly up, not at the end of each tick

- [ ] **Z1** `CHANGELOG.md` — a `0.4.0` section with a **completeness factor on every feature**.
      A feature listed without one reads as finished.
- [ ] **Z2** `docs/STATUS.md` — what is now true, the defects worth remembering, and the operator
      actions this release created. Include the new flag console and what it can and cannot do.
- [ ] **Z3** Verify the deployed sha. `curl https://commissionwatch.bmux.sh/api/version` — main
      auto-deploys, so verify against production rather than against CI.

---

## Tick log

Append one line per tick: time, what the subagent was given, what landed. Keep it terse.

- **07:0x — tick 1.** Loop armed (cron `*/15 * * * *`, job `83ae0c64`). Killed two stale test shells
  orphaned from an earlier session (19h and 13h old, 0s CPU, one holding a Postgres connection — a
  `node:test` pool that was never destroyed). Working tree was clean, so neither had left a partial
  edit. Spec + plan committed as `f9db1ec`. Dev subagent dispatched on F1a–F1c.
